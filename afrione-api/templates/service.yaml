apiVersion: v1
kind: Service
metadata:
  name: {{ include "afrione-api.fullname" . }}
  labels:
    {{- include "afrione-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "afrione-api.selectorLabels" . | nindent 4 }}
