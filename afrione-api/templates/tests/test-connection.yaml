apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "afrione-api.fullname" . }}-test-connection"
  labels:
    {{- include "afrione-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "afrione-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
